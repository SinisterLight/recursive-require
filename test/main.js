var should  = require('should');

describe('recusive-reqiure', function(){
  it('should require js in a folder path and map it to object with attribute with file name');
  it('should ignore non-js file');
  it('should require js in sub folders and create an object tree for files');
});

